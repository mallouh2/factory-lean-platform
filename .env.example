# Server-only. Use DIFFERENT Supabase projects for development, preview/testing and production.
APP_ENV=development
APP_ORIGIN=http://localhost:3000
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Provisioning script only; never needed by the web application.
SUPABASE_SECRET_KEY=
# Development demo provisioning only. Never configure these in production.
DEMO_EMAIL=
DEMO_PASSWORD=
